# golangci-lint configuration for Burrow
# https://golangci-lint.run/usage/configuration/

version: "2"

run:
  timeout: 5m
  modules-download-mode: readonly

linters:
  enable:
    # Default linters
    - govet
    - staticcheck
    - errcheck
    - ineffassign
    - unused
    # modernize is intentionally disabled to avoid expanding CI lint scope
    # while stabilizing the GitHub Actions toolchain.

  settings:
    govet:
      enable-all: true
      disable:
        - shadow
        - fieldalignment # struct field alignment optimization is noisy
    errcheck:
      exclude-functions:
        - (io.Closer).Close
        - (*os/exec.Cmd).Run
        - (*os/exec.Cmd).Start
    staticcheck:
      checks: ["all", "-QF1003", "-SA9003"]

  exclusions:
    rules:
      # Ignore certain patterns in test files
      - path: _test\.go
        linters:
          - errcheck
      # Ignore errors from os.Remove in cleanup code
      - text: "os.Remove"
        linters:
          - errcheck
