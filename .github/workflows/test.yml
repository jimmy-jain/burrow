name: test

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main, dev]

jobs:
  test:
    # Run on macos-14 (arm64) for regular pushes.
    # macos-13 (amd64) is covered by the release workflow build matrix.
    runs-on: macos-14
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.25"

      - name: Install bats-core
        run: brew install bats-core

      - name: Run tests
        run: ./scripts/test.sh
