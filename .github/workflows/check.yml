name: check

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main, dev]

jobs:
  check:
    runs-on: macos-14
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.25"

      - name: Install tools
        run: |
          brew install shfmt shellcheck
          go install golang.org/x/tools/cmd/goimports@latest

      - name: Run shell + syntax checks
        run: ./scripts/check.sh --no-format

      - name: Run golangci-lint
        uses: golangci/golangci-lint-action@v7
        with:
          version: v2.1.6
          args: --config .golangci.yml ./cmd/...
